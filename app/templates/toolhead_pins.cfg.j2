# Toolhead board alias map generated by KlippConfig
{% if toolhead_profile %}
[board_pins toolhead]
mcu: toolhead
aliases:
{% for pin_name, pin_value in toolhead_profile.pins|dictsort %}
  {{ pin_name }}={{ pin_value.replace('toolhead:', '') }}
{% endfor %}
{% else %}
# Toolhead profile was not found; aliases unavailable.
{% endif %}


