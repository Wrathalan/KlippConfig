# Next Steps

Generated by KlippConfig for **{{ preset.name }}** on board **{{ project.board }}**.

## Deploy
1. Copy these files to your Klipper config directory.
2. Ensure `printer.cfg` is the active root config.
3. Restart Klipper.

## Mandatory checks before printing
1. Confirm all pin mappings and motor directions.
2. Confirm endstop/probe trigger state.
3. Run PID and motion calibration routines.

## Suggested order
1. `STATUS` and `FIRMWARE_RESTART`
2. `QUERY_ENDSTOPS`
3. Axis homing
4. PID tuning
5. Input shaper calibration
6. First layer tuning


